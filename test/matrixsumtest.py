import numpy as np
import cv2
import copy


# img = cv2.imread("asd.jpg")
# sum_matrix = np.sum(img, axis=2)  # 按z轴方向求和
# print(img.dtype)
# print(sum_matrix.dtype)
# sum_matrix = np.stack((sum_matrix, sum_matrix, sum_matrix), axis=2).astype(np.uint8)
# out = np.hstack([img, sum_matrix])
# out = cv2.resize(out, (int(out.shape[1] / 2), int(out.shape[0] / 2)))
# cv2.imshow("out", out)
# cv2.waitKey(0)
# img = cv2.imread("asd.jpg")
# img2 = copy.deepcopy(img)

# img2 *= 0
# cv2.imshow("img2", img2)
# cv2.waitKey(0)
print(str(1 - 0.0)) 